FROM python:3.12-slim
RUN apt-get update && apt-get install -y --no-install-recommends \
        rtl-sdr \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /app
COPY rf_listener.py .
ENTRYPOINT ["python3", "rf_listener.py"]
